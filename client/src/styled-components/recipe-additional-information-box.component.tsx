import { styled } from '@mui/system';
import { Box, BoxProps } from '@mui/material';

const StyledRecipeAdditionalInformationBox = styled(Box)<BoxProps>({
    display: 'flex',
    alignItems: "center",
    marginTop: "1rem",
    marginBottom: "1rem",
});

export default StyledRecipeAdditionalInformationBox;