{
  "name": "json-server",
  "version": "1.0.0",
  "description": "Server mocking for frontend development",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
